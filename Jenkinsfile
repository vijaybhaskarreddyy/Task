inode {
    // some block
    stage('git') {
    // some block
     git 'https://github.com/vijaybhaskarreddyy/spring-petclinic.git'
    }
    stage('build') {
    // some block
    sh '''mvn clean install'''
   }
}
